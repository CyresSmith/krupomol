'use client';

import { Section } from '@components/shared';

export const ProductInfo = () => {
    return (
        <Section>
            <div className="container">blank</div>
        </Section>
    );
};
